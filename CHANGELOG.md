# 변경 이력

버전은 `package.json` 의 `version` 과 `v<버전>` 태그를 따른다. 배포 절차는 [docs/DEPLOY.md](docs/DEPLOY.md).

## 0.2.0 — 2026-09-19

### 기능

- **본문 찾기**(`Ctrl+F`, 보기 탭): 문서 전체에서 찾아 일치한 곳을 오가고 글자 층에 강조한다. 띄어쓰기·대소문자 무시.
  쪽의 글자는 찾을 때 처음 읽으므로 열기·쪽 전환 속도에는 영향이 없다.
- **삽입 항목 재편집**: 저장할 때 텍스트·펜 선의 기록을 쪽의 `/PieceInfo` 에 함께 적어, 이 앱에서 다시 열면 편집 가능한 항목으로 되살린다.
  저장을 거듭해도 콘텐츠 스트림·폰트가 쌓이지 않는다. 기록이 없는 파일은 문서 정보만 확인한다(열기 시간 +3–5ms, 196MB·500쪽 샘플).
- **용량 최적화**가 폼 XObject 안의 이미지까지 살핀다.
- **전자서명이 있는 문서**는 파일 탭에 표시하고 저장 전에 문서마다 한 번 확인한다.
- **캡처 목록 보호**: 이미지로 저장하지 않은 캡처가 있으면 닫기·새로 열기·창 닫기 전에 알린다. 캡처만 한 경우에는 PDF 를 "변경됨" 으로 치지 않는다.
- 원본에 덮어써서 저장본으로 다시 열릴 때 보던 쪽·분할 그룹·캡처 목록이 유지된다(이전에는 모두 사라졌다).

### 저장소

- MIT 라이선스, 제3자 고지(`THIRD-PARTY-NOTICES.md`), Pretendard 의 OFL 전문을 배포본에 포함.
- GitHub Actions: CI(타입 검사·단위·E2E)와 GitHub Pages 배포 워크플로. 배포 계획 문서(`docs/DEPLOY.md`).
- `engines`(Node ≥ 22.12)와 `.nvmrc`(24).

### 테스트·도구

- 단위 테스트: `textLayout`, `plan`, `editsCodec`, `search`.
- E2E: 무손실 검증(이미지·폰트 스트림 해시 비교), 재편집, 본문 찾기, 서명 확인·캡처 보호·덮어쓰기 후 이어받기, 폼 XObject 최적화.
- `npm run bench:open`: 브라우저에서 열기 → 첫 화면 시간(통째 읽기 / 구간 읽기 청크 크기별). 측정 결과 청크 크기는 의미 있는 차이가 없어 8MB 를 유지한다.
- 부하가 걸리면 글자를 놓치던 캡처 E2E 의 경합을 고쳤다. CI 에서는 실패한 테스트를 한 번 재시도한다.
- E2E 가 Windows 에서 끝나지 않던 문제를 고쳤다: 브라우저를 별도 프로세스의 서버로 한 번만 띄우고 워커는 접속만 한다
  (워커가 끝날 때 Playwright 가 잠긴 임시 프로필을 지우느라 멈추던 것이 원인). 부하 시 흔들림을 줄이려 expect 대기를 15초로 늘렸다.
- 배포 워크플로는 main 에서 수동 실행한다(`github-pages` 환경이 main 의 배포만 허용한다).

## 0.1.0

- 초기 구현: 열기·병합·무손실 저장·인쇄, 일괄 분할, 영역 캡처, 페이지 조작, 텍스트·펜 삽입, 용량 최적화 저장, 도움말, PWA·오프라인.
