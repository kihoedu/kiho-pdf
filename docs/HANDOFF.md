# 인수인계 · 작업 기록

여러 데스크탑에서 여러 작업자(사람·AI 에이전트)가 이어서 작업하기 위한 문서다.
**작업을 시작할 때 1~3절을 읽고, 끝낼 때 2절을 고치고 6절 맨 위에 기록을 추가한다.**

| 무엇을 알고 싶은가 | 볼 곳 |
|---|---|
| 지켜야 할 규칙(도움말 동기화 · 무손실 · 속도), 검증 명령 | [CLAUDE.md](../CLAUDE.md) |
| 기능·화면·구조·벤치마크 | [README.md](../README.md) |
| 버전별로 바뀐 것 | [CHANGELOG.md](../CHANGELOG.md) |
| 배포 방법 | [DEPLOY.md](DEPLOY.md) |
| **지금 어디까지 했고, 다음에 무엇을 하며, 무엇을 조심해야 하는가** | 이 문서 |

이 문서에는 위 문서들의 내용을 옮겨 적지 않는다. 코드·git 기록으로 알 수 없는 것(결정의 이유, 미해결 사항, 기기별 함정)만 적는다.

---

## 1. 새 기기에서 시작하기

```bash
git clone https://github.com/kihoedu/kiho-pdf.git && cd kiho-pdf
npm ci                       # postinstall 이 PDF.js 자산·폰트를 public/ 으로 복사한다(public/pdfjs, public/fonts 는 git 에 없다)
npm run dev                  # http://localhost:5173
npm run typecheck && npm test && npm run e2e
```

- Node.js 24 권장(`.nvmrc`), 22.12 이상이면 된다. 2026-09-19 의 작업 기기는 22.23 이었고 문제없었다.
- E2E 는 **설치된 Edge** 를 쓴다(Chrome 은 `PW_CHANNEL=chrome`). 브라우저를 내려받지 않는다. 전체 21개, 약 1분.
- **`npm install` 로 `package-lock.json` 을 다시 쓰지 않는다.** npm 버전이 낮으면 선택적 의존성의 `libc` 항목이 통째로 빠진다
  (2026-09-19 에 실제로 겪음). 의존성을 바꾸지 않았다면 `npm ci` 만 쓰고, 버전 같은 값만 고칠 때는 lock 파일의 해당 줄만 손으로 고친다.
- 줄바꿈: 이 저장소의 Windows 작업본은 CRLF 다(`core.autocrlf=true`). 스크립트로 파일을 고칠 때 LF 를 섞지 않는다.
- 생성물은 git 에 없다: `dist/`, `public/pdfjs/`, `public/fonts/`, `bench/out/`, `bench/samples/`, `test-results/`.
  `bench/samples/test_12p.pdf` 는 E2E 가 없으면 자동으로 만든다. 196MB 샘플은 `npm run bench:sample`.

## 2. 현재 상태 (2026-09-20, 썸네일 개선 시점)

| 항목 | 값 |
|---|---|
| 브랜치 | `main` = `origin/main`, 작업본 깨끗함. 마지막 코드 변경은 **썸네일 빠른 경로**(6절 맨 위 기록) |
| 버전 · 태그 | 0.2.0 · `v0.2.0` → `4452e99`. 그 뒤의 변경은 CHANGELOG 의 "미출시" 에 있다(버전은 올리지 않았다) |
| 배포 | https://kihoedu.github.io/kiho-pdf/ — 2026-09-20 에 main 으로 다시 배포했다(본문 찾기 제거 + 썸네일 개선 반영) |
| CI | 6절 맨 위 기록의 커밋에서 통과 — Linux·Chrome, 타입 검사 · 단위 70 · E2E 22 |
| 로컬 검증 | Windows·Edge 에서 타입 검사 · 단위 70 · `npm run e2e` 22개 통과 |
| 진행 중인 작업 | 없음 |

저장소 설정(코드 밖에 있어 git 으로는 알 수 없는 것):

- GitHub Pages: Source = **GitHub Actions**, HTTPS 강제. 저장소는 **공개**(무료 플랜의 Pages 조건).
- `github-pages` 환경은 **`main` 브랜치의 배포만 허용**한다. 태그·다른 브랜치에서 배포 워크플로를 돌리면 거부된다.
  그래서 배포는 `gh workflow run "Deploy (GitHub Pages)" --ref main` 으로 한다(자동 배포 아님).
- 워크플로 파일을 푸시하려면 토큰에 `workflow` 권한이 있어야 한다.

## 3. 열려 있는 일

### 3-1. 소유자(프로젝트 주인)의 결정이 필요한 것

| # | 내용 | 지금 상태 |
|---|---|---|
| D1 | **라이선스**: 에이전트가 관례에 따라 MIT, 저작권자 "Eunkwang Choi" 로 정해 공개했다. 소유자가 명시적으로 고른 것이 아니다 | 이의가 없으면 그대로. 바꾸려면 `LICENSE`, `package.json` 의 `license`, README 마지막 절 |
| ~~D2~~ | **본문 찾기(Ctrl+F)** — **결정됨(2026-09-19): 뺀다.** 2026-09-18 소유자 지시("[검색] 메뉴는 제거한다")로 지웠던 것을 0.2.0 이 보기 탭 안에 되살렸으나, 소유자가 원래 의도대로 빼기로 했다 | 코드·테스트·도움말에서 제거 완료(6절 2026-09-19 두 번째 기록). **다시 넣지 않는다** |
| D3 | **공개 주소**: PWA·서비스 워커는 주소(출처)별로 설치된다. 사용자 지정 도메인을 쓸 계획이면 사용자가 늘기 전에 옮기는 편이 낫다 | `kihoedu.github.io/kiho-pdf` 사용 중 |
| ~~D4~~ | **썸네일 표시 속도** — **결정됨(2026-09-20): 개선하라.** 제안 ①(스캔 쪽 빠른 경로)과 ②(스크롤 방향 선행 렌더)를 넣었다 | 완료. ③(IndexedDB 영구 캐시)은 넣지 않았다 — 3-2 의 2번 |

### 3-2. 다음 작업 후보 (우선순위 순)

1. **[할 일] bench 스크립트의 종료 지연을 고친다 — 고친 뒤 이 항목을 목록에서 지운다.**
   - 대상은 세 개다: `bench/shot.mjs`, `bench/open-bench.mjs`, `bench/thumb-bench.mjs`. 셋 다 브라우저를 직접 띄우고
     `browser.close()` 를 기다리므로, 4절의 임시 프로필 문제로 Windows 에서 가끔(약 3번에 1번) 끝에서 오래 걸린다. 결과는 이미 출력된 뒤라 측정값·캡처에는 영향이 없다.
   - 고치는 방법: E2E 와 같은 방식(`e2e/browser-server.mjs` 처럼 서버 프로세스로 띄우고 끝나면 트리째 종료)으로 하거나,
     결과를 출력한 뒤 닫기를 기다리지 않고 브라우저 프로세스를 끝내고 `process.exit` 한다. 세 스크립트가 같은 코드를 쓰도록 공통 도우미로 뺀다.
   - 끝났다고 보는 기준: Windows 에서 세 스크립트를 각각 5회 이상 돌려 매번 결과 출력 직후 몇 초 안에 끝나고, 남는 `node`·브라우저 프로세스가 없다.
   - 끝낸 사람이 할 일: **이 항목(3-2 의 1번)을 지우고** 뒤 항목의 번호를 당긴다. 4절 "E2E · 테스트" 첫 항목과 6절 작업 기록에서
     "bench 스크립트는 아직 그대로"라는 취지의 문장이 있으면 함께 고치고, 6절에 기록을 남긴다. CHANGELOG 의 "테스트·도구"에도 한 줄 적는다.
2. **썸네일: 남은 개선 여지.** 빠른 경로(①)와 선행 렌더(②)는 넣었다. 아직 안 한 것:
   - **③ IndexedDB 영구 캐시**: 같은 파일을 다시 열면 썸네일을 즉시 보여 준다. 500쪽 문서 하나에 약 4MB. 파일 동일성 판정(이름·크기·수정시각)과
     용량 관리(오래된 것부터 버리기)가 함께 필요하다.
   - 빠른 경로가 닿지 않는 스캔본: 1비트 흑백(CCITT·JBIG2)과 CMYK JPEG. 전자는 브라우저가 디코딩하지 못하고, 후자는 색이 브라우저마다 달라 뺐다.
     JBIG2·CCITT 는 PDF.js 의 디코더를 워커에서 직접 부르는 방법이 있을 수 있다(미조사).
   - 빠른 경로는 `bench:thumb` 기준 23.7 ms/쪽이고 순수 디코딩만 하면 18.4 ms/쪽이다(C 줄). 차이는 워커 왕복·Blob 복사다.
3. 빌드 경고: 번들 청크가 500KB 를 넘는다(vite 경고). 첫 로딩에만 영향이 있고 PWA 캐시 뒤에는 무관하다. 손대려면 규칙 3에 따라 전후를 잰다.
4. GitHub Actions 경고: `actions/*@v4` 가 Node 20 기반이라는 폐기 예고, `ubuntu-latest` 가 2026-10-19 부터 Ubuntu 26 으로 바뀐다는 예고. 지금은 동작한다.
5. 재편집 표시는 "편집이 있는 쪽이 하나라도 있는 저장"에서만 문서 정보에 남는다. 표시 없이 쪽의 기록만 남은 파일(다른 도구가 Info 를 지운 경우 등)은
   되살리지 않는다. 드문 경우라 그대로 두었다.

## 4. 함정 — 같은 데서 시간을 버리지 않도록

### E2E · 테스트

- **E2E 의 브라우저는 별도 프로세스 서버다**(`e2e/global-setup.ts` → `e2e/browser-server.mjs`, 워커는 `connectOptions` 로 접속). **되돌리지 않는다.**
  워커가 브라우저를 직접 띄우면, 워커가 끝날 때 Playwright 가 임시 프로필(`%TEMP%\playwright_chromiumdev_profile-*`)을 지운다.
  Windows 에서는 방금 닫힌 프로필의 파일이 약 3번에 1번꼴로 "삭제 대기"로 묶이는데(같은 사용자의 프로세스는 핸들을 쥐고 있지 않았다 —
  ESET 같은 시스템 쪽으로 추정, 미확인), Playwright 가 파일 150여 개를 10번씩 재시도하느라 워커가 끝나지 않는다.
  증상은 "app 테스트가 다 끝났는데 pwa 테스트가 시작되지 않고 러너가 요약 줄 없이 남는다"였다.
  - 처음에는 "Edge 가 워커를 못 닫는다"고 **잘못 진단**했다. Edge 프로세스는 정상 종료된다. Chrome 도 마지막 정리에서 같은 식으로 남았다.
  - 빈 페이지만 열고 닫아도 재현된다(headless). 앱 코드와 무관하다.
- 워커들이 브라우저 하나를 나눠 쓰므로 부하가 몰리면 느리다. 그래서 `expect.timeout` 이 15초다. 5초로 되돌리면 재편집 테스트가 흔들린다.
- **E2E 가 도는 동안 `src/` 를 고치지 않는다.** 개발 서버의 HMR 이 테스트 중인 페이지를 다시 읽어 엉뚱한 실패가 난다.
- 에이전트가 E2E 를 돌릴 때: 파이프(`| tail`)나 짧은 셸 타임아웃으로 끊으면 node·vite 프로세스가 고아로 남아 다음 실행(포트 5173/4173)을 방해한다.
  백그라운드 + 로그 파일로 돌리고, 남은 것은 CommandLine 에 `kiho-pdf` 가 든 `node.exe` 만 골라 `taskkill /T /F` 한다. **사용자의 Edge 창은 건드리지 않는다.**
- 이 앱이 저장한 파일을 다시 열면 삽입 항목이 오버레이로 되살아난다. "본문에 실제로 그려졌는가"를 검증하는 테스트는 열기 전에
  `window.__kihoTune = { noRestore: true }` 를 넣어야 한다(`src/devTune.ts`, 개발 서버 전용 — 빌드본을 쓰는 `*.pwa.ts` 에서는 듣지 않는다).
- `mockPickers` 는 `window.confirm` 을 항상 승인으로 덮어쓴다. 확인 창의 문구·거절을 검증하려면 `e2e/guard.e2e.ts` 의 `recordConfirms` 를 쓴다
  (나중에 덮어써도 남도록 `defineProperty` 로 고정해 둔 것이다).
- `e2e/optimize.e2e.ts` 는 큰 JPEG 을 base64 로 주고받아 원래 오래 걸린다(20초~1분). 고장이 아니다.
- CI 에서는 실패한 테스트를 한 번 재시도한다(`retries`). 로컬에는 재시도가 없어 **부하로 흔들린 테스트가 그대로 실패로 남는다.**
  캡처 미리보기(Linux 러너)와 재편집(`reedit`, Windows)이 각각 한 번씩 그렇게 실패했다. 재편집은 무거운 `optimize` 와 나란히 돌 때
  32.9초가 걸리며 저장 알림 단언에서 떨어졌고, 단독으로 돌리면 5.9초에 통과한다. 실패하면 **그 파일만 단독으로 다시 돌려** 부하 때문인지 먼저 가린다.
- **앱이 떠 있는 채로 `src/` 를 고치면 Vite HMR 이 모듈 사본을 만든다.** 그 상태에서 bench 가 `import('/src/pdf/caches.ts')` 같은 식으로
  모듈을 직접 가져오면 앱이 쓰는 것과 **다른 인스턴스**를 잡아(캐시가 서로 달라) 측정이 0ms 로 나오는 등 엉뚱한 값이 된다.
  그래서 bench 는 앱이 `window.__kiho` 로 내보낸 것만 쓴다(`src/main.tsx`, 개발 전용). 새 측정 항목을 넣을 때도 이 훅에 추가한다.
  고친 뒤에는 개발 서버를 다시 띄우고 재는 편이 안전하다.
- 텍스트 도구로 클릭한 직후 바로 타이핑하면 안 된다. 상자는 폰트를 받은 뒤에 생기므로 `.text-box textarea` 가 포커스될 때까지 기다린다.

### 코드

- 도움말 E2E(`e2e/help.e2e.ts`)와 `src/help/guide.test.ts` 는 "없어진 기능이 도움말·단축키에 남아 있지 않은지"도 본다. 기능을 넣고 뺄 때 함께 고친다.
- 폰트 패키지는 **`@cantoo/pdf-lib` 에 `@cantoo/fontkit`** 을 짝으로 쓴다. 예전 `@pdf-lib/fontkit` 으로 바꾸면 서브셋 임베딩이
  `Cannot read properties of undefined (reading 'pos')` 로 깨진다(두 패키지의 `encode()` 규약이 다르다). 2026-09-18 에 겪고 교체했다.
- 단축키를 넣을 때는 `src/App.tsx` 의 비교식 모양(`mod && key === 'f'`)을 지킨다. `guide.test.ts` 가 정규식으로 이 모양을 읽어 도움말 표와 대조한다.
- 실행 취소 기록을 남기는 편집은 모두 `store.ts` 의 `commit()` 을 거친다. 캡처 목록만 바꾸는 편집은 PDF 를 "변경됨" 으로 만들지 않고
  `capturesUnsaved` 로 따로 센다(닫기 전 경고용).
- 저장 엔진(Worker)은 원본을 한 번만 파싱해 `src.id` 로 캐시한다. 재편집 복원은 **그 캐시된 문서를 직접 고친다** — 복원 뒤의 `Source.file` 은
  그려 넣은 부분을 걷어 낸 새 `File` 이고, 암호는 지운다(다시 쓴 PDF 에는 암호가 없다).
- pdf-lib 가 쪽에 그릴 때 끼우는 `q`/`Q` 스트림은 **문서 전체가 하나를 공유**한다. 복원은 `/Contents` 배열에서 참조만 빼고 객체는 지우지 않는다.
- 폰트 사전은 여러 쪽이 함께 쓸 수 있다. 복원에 실패한 쪽이 하나라도 있으면 추가했던 폰트 이름을 지우지 않는다(그 쪽의 글자가 그 폰트를 쓴다).
- 전자서명 확인 창은 저장 대화상자 **앞**에 뜬다. 확인 창에 오래 머물면 브라우저가 "사용자 동작" 권한을 거둬 저장 대화상자가 막힐 수 있어,
  그때는 "한 번 더 눌러 주세요"로 안내하고 승인은 기억해 둔다(`io.ts` 의 `errText`, `signatureAck`).

## 5. 결정과 그 이유

| 결정 | 이유 | 되돌리려면 |
|---|---|---|
| **본문 찾기를 넣지 않는다**(2026-09-19 소유자 결정) | 2026-09-18 "[검색] 메뉴는 제거한다" 지시의 원래 의도. 0.2.0 이 보기 탭에 되살렸다가 다시 뺐다 | 되돌린 커밋을 되짚으면 되지만, **소유자 승인 없이 다시 넣지 않는다**. 글자 층(선택·복사)은 그대로 둔다 |
| 재편집 기록은 쪽의 `/PieceInfo` + 문서 정보의 `KihoEdits` 표시 | `/PieceInfo` 는 응용 프로그램 전용 자료를 두는 PDF 표준 자리. 표시를 Info 에 둔 것은 PDF.js 의 `getMetadata()` 한 번(3~5ms)으로 확인하려고 — 표시 없는 파일은 여는 속도가 그대로 | `engine/pieceInfo.ts`, `model/editsCodec.ts` |
| 파일 안의 기록은 엄격히 검증하고, 어긋나면 그 쪽은 건드리지 않음 | 파일은 믿을 수 없는 입력. 되살리지 못해도 **그린 내용이 사라지면 안 된다** | — |
| 대용량 열기(구간 읽기 청크)는 바꾸지 않음 | 측정: 8/32/64MB 가 1.25/1.35/1.27초로 차이 없음(다른 기기에서 0.62/0.61/0.62초 — 역시 차이 없음). 평평한 페이지 트리는 어차피 파일 전체를 훑는다 | README 벤치마크 절 |
| 서명 여부는 AcroForm 의 SigFlags(PDF.js `IsSignaturesPresent`)로 판단 | 열 때 이미 부르는 `getMetadata()` 에 들어 있어 추가 비용 없음 | — |
| 덮어쓰기 후 다시 열 때 분할 그룹·캡처·보던 쪽을 이어받음 | 이전에는 저장 한 번에 목록이 모두 사라졌다(데이터 손실). 문서 전체를 저장한 경우에만(쪽 구성이 같으므로) | `io.ts` 의 `reopenSaved` |
| 썸네일은 "쪽 전체를 덮는 이미지 한 장"인 쪽만 빠른 경로로 | 스캔본이 거의 그 꼴이고, 그 경우에만 이미지를 그대로 줄여도 화면과 같아진다. 조건을 하나라도 못 맞추면 PDF.js 로 넘긴다 | `src/engine/pageImage.ts` 의 `fullPageImage` 가 항상 undefined 를 돌려주게 하면 전체가 예전 경로로 돌아간다 |
| PDF.js 워커를 여러 개 쓰지 않는다 | 측정: 40.5 ms/쪽(워커 4개)로 조금 빨라지지만 메인 스레드 막힘이 343 → 774ms 로 나빠지고 메모리도 배로 든다 | — |
| 빠른 경로는 쪽당 120KB 이상인 문서에서만 시도 | 글자 위주 문서까지 pdf-lib 로 파싱하면(메모리·시간) 얻는 것 없이 손해다. 맞은 적 없이 4번 빗나가면 그 문서에서는 그만 묻는다 | `src/pdf/thumbs.ts` 의 `MIN_BYTES_PER_PAGE`·`GIVE_UP_AFTER` |
| 썸네일 동시성을 경로에 따라 다르게(PDF.js 2, 빠른 경로 4) | 측정: PDF.js 는 캔버스가 메인 스레드를 써서 4로 올리면 막힘이 173 → 520ms 로 나빠지고, 빠른 경로는 메인 스레드를 안 써서 올릴수록 빨라진다(28.9 → 21.4 ms/쪽) | `src/pdf/caches.ts` 의 `THUMB_CONCURRENCY` |
| 배포는 main 에서 수동 실행 | `github-pages` 환경이 main 만 허용. main 에 올릴 때마다 자동 배포하지 않는 것은 검증 없이 나가는 것을 막으려고 | `deploy.yml` 의 `on:` |
| 0.2.0 작업을 브랜치·PR 없이 main 에 직접 커밋 | 1인 저장소였고 배포가 main 에서만 가능. **여러 명이 작업하는 지금부터는 7절의 규칙을 따른다** | — |

## 6. 작업 기록 (최신이 위)

### 2026-09-20 · Windows 11 데스크탑(`D:\Github\kiho-pdf`) · Claude Code(Opus 5), 지시: Eunkwang Choi

| 커밋 | 내용 |
|---|---|
| (이 커밋) | 썸네일 빠른 경로 + 스크롤 방향 선행 렌더. 배포까지 |

한 일: 3-1 의 D4(썸네일 개선)를 소유자가 승인해 ①·②를 넣었다. 무엇이 바뀌었는지는 CHANGELOG "미출시", 결정의 이유는 5절.

- **빠른 경로**: 저장 워커가 "쪽 전체를 덮는 이미지 한 장"인 쪽을 가려내(`engine/pageImage.ts`) 그 JPEG 을
  *디코딩하면서 바로 축소*해 작은 비트맵만 넘긴다(`engine/worker.ts` 의 `thumb`). 조건에 안 맞으면 지금까지대로 PDF.js 로 그린다.
  두 경로를 고르는 곳은 `pdf/thumbs.ts` 한 곳이다.
- **측정**(`bench:thumb`, 300dpi 스캔본 40쪽): 53.0 → 23.7 ms/쪽, 메인 스레드 막힘 211 → 0ms.
  `bench:thumb` 에 "앱의 실제 경로" 비교(D 줄)를 넣었고, 개발용 스위치 `__kihoTune.noFastThumb` 로 끄고 켠다.
- **정확도**: `e2e/thumbs.e2e.ts` 가 두 경로의 결과를 8×8 밝기 격자로 견준다(방향·잘림에는 민감하고 축소 방식 차이에는 둔하다).
  일부러 회전을 빼거나 180° 돌리거나 다른 쪽을 그리게 해 보고 테스트가 실제로 실패하는지 확인했다.

남긴 것·겪은 것:

- **내가 넣었다가 고친 버그**: 스크롤 핸들러를 `setScrollTop((prev) => … e.currentTarget.scrollTop …)` 로 썼더니
  상태 갱신 함수가 나중에 불릴 때 `currentTarget` 이 null 이라 썸네일 창 전체가 죽었다. 값을 핸들러 안에서 미리 꺼내 쓰면 된다.
  E2E(`core.e2e.ts` 분할)가 잡았지만, 그 전에 bench 로그의 `pageerror` 줄을 흘려보낸 탓에 한참 늦게 알아챘다 — **pageerror 는 그냥 넘기지 말 것.**
- 벤치가 0ms 처럼 말이 안 되는 값을 내면 4절의 "HMR 모듈 사본" 항목을 먼저 본다(여기서 오래 헤맸다).
- 빠른 경로는 스캔본에만 해당한다. 글자 위주 문서는 예전과 같은 속도다(쪽당 120KB 미만이면 시도조차 하지 않는다).
- 남은 개선 여지는 3-2 의 2번(IndexedDB 캐시, 1비트 흑백·CMYK 스캔).

### 2026-09-19 · Windows 11 데스크탑(`D:\Github\kiho-pdf`) · Claude Code, 지시: Eunkwang Choi

| 커밋 | 내용 |
|---|---|
| `631d233` | 본문 찾기 제거 + 0.1.0 을 만든 기기의 기록으로 문서의 사실관계를 바로잡음 |

한 일: `origin/main` 을 받아(`d777e24`→`41f1fcc`) 이 기기를 0.2.0 에 맞춘 뒤, 소유자 결정에 따라 본문 찾기를 빼고,
**0.1.0 을 만든 대화의 기록으로** 문서를 고쳤다.
0.1.0 작업 기기가 여기(`D:`)라 그 경위는 이 기기의 작업자만 알고 있었다.

- **본문 찾기를 뺐다**(소유자 결정 — 3-1 의 D2). 0.2.0 이 보기 탭에 넣었던 것을 5절의 되돌리기 목록대로 걷어냈다:
  `model/search.ts`·`actions/search.ts`(+단위 테스트)·`e2e/find.e2e.ts` 삭제, `FindSection`·강조 effect·`getPageCompact`·
  `store` 의 찾기 상태·`mod+f`·SHORTCUTS 2줄·`::highlight` 제거, 도움말·README·CHANGELOG 반영.
  글자 층은 그대로라 **본문 글자 선택·복사는 된다**(`annot.e2e.ts` 가 "찾기 칸이 없다"까지 함께 단언한다).
  버전은 올리지 않았고 CHANGELOG 에 "미출시" 로 적었다. **배포된 주소에는 아직 찾기가 남아 있다 — 다시 배포해야 반영된다.**
- 3-1 의 D2(본문 찾기를 뺀 이유)를 "모른다" → 소유자 지시였음으로 정정하고, 아래 2026-09-18 기록에 근거를 남겼다.
- 3-2 의 옛 2번(벤치마크 표 재측정)은 원인을 찾아 해결했다: 두 수치는 **재는 대상이 달랐다**. 0.45초는 앱 안에서 잰 값이라
  파일이 이미 메모리에 있었고, `bench:open` 은 OPFS(디스크)에서 읽는 시간을 포함한다. 이 기기에서 `bench:open` 을 다시 재니
  통째 읽기 0.70초 · 구간 읽기 8/32/64MB 가 0.62/0.61/0.62초였다(다른 기기는 1.19/1.25/1.35/1.27초 — 기기 차이).
  청크 크기가 의미 없다는 결론은 두 기기에서 같아 `RANGE_CHUNK` 는 그대로 둔다. README 벤치마크 표를 `bench:open` 기준으로 통일하고
  줄마다 어떤 샘플·조건인지 밝혔다(12쪽 저장 시간이 196MB 샘플의 값처럼 묶여 있던 것도 바로잡음).
- 그 자리에 **썸네일 개선**(2026-09-18 에 측정만 하고 미구현)을 3-2 의 2번과 3-1 의 D4 로 올렸다.
- 4절에 fontkit 짝 함정을 추가했다.

검증: `npm ci` 후 타입 검사 통과 · 단위 43개(찾기 단위 테스트 9개가 빠졌다) · E2E 20개(Windows·Edge). `bench:open` 3회.
첫 E2E 실행에서 `reedit` 하나가 부하 때문에 흔들려 실패했고, 단독 재실행과 두 번째 전체 실행에서 통과했다(4절에 적어 두었다).
남긴 것: **배포를 하지 않았다** — 공개 주소에는 아직 본문 찾기가 있다. 썸네일 개선은 여전히 승인 대기.

### 2026-09-19 · Windows 11 데스크탑(`F:\Manuscripts\GitHub\kiho-pdf`) · Claude Code(Fable 5.1), 지시: Eunkwang Choi

| 커밋 | 내용 |
|---|---|
| (문서 커밋) | 이 문서(`docs/HANDOFF.md`) 추가, CLAUDE.md·README 에서 연결 |
| `c68fdab` | E2E 가 Windows 에서 끝나지 않던 문제 수정(브라우저 서버 분리), `expect.timeout` 15초 |
| `4452e99` | 배포 워크플로를 main 수동 실행으로, 캡처 E2E 보강, CI 재시도 1회. `v0.2.0` 태그 |
| `9c6cae1` | 0.2.0 전체: 아래 목록 |

한 일(처음에 문서를 검토해 만든 20개 항목을 순서대로 진행):

- **문서**: README 전면 보완(파일·보기·페이지 탭, PWA, 단축키 표, 스크립트, 구조, 벤치마크 절차), CLAUDE.md 검증 절 보강.
- **저장소**: MIT 라이선스, `THIRD-PARTY-NOTICES.md`, Pretendard OFL 전문을 배포본에 포함, CI·배포 워크플로, `docs/DEPLOY.md`
  (GitHub Pages · Firebase Hosting · Cloud Storage+LB · FTP 비교와 절차), `.nvmrc`·`engines`, `CHANGELOG.md`, 버전 0.2.0.
- **테스트**: 단위 4종(`textLayout`, `plan`, `editsCodec`, `search`), E2E 5종(`lossless`, `reedit`, `find`, `guard`, 폼 XObject 최적화).
- **기능**: 본문 찾기(2026-09-19 에 소유자 결정으로 제거 — 3-1 의 D2) · 삽입 항목 재편집 · 폼 XObject 안 이미지 최적화 · 전자서명 저장 전 확인 · 캡처 미저장 경고 ·
  덮어쓰기 후 상태 이어받기. 도움말(`guide.ts`, `ui.ts`)과 README 를 같은 커밋에서 고쳤다.
- **측정**: `npm run bench:open` 추가. 구간 읽기 청크 크기 비교(변경 불필요로 결론), 문서 정보 확인의 열기 비용 3~5ms.
- **배포**: GitHub Pages 첫 배포. 배포 주소에서 첫 화면 · 콘솔 오류 없음 · HTTPS · 서비스 워커 등록 · 폴더 저장 API · MIME 을 확인.

검증: 타입 검사 통과, 단위 52개, E2E 21개(Windows·Edge 2회, Linux·Chrome CI).

남긴 것: 3절 전부. 이 기기의 `%TEMP%` 에 지워지지 않는 `playwright_chromiumdev_profile-*` 폴더 40여 개(개당 약 8MB) — 재부팅 뒤 지울 수 있다.

### 2026-09-18 · Windows 11 데스크탑(`D:\Github\kiho-pdf`) · Claude Code(Fable 5.1), 지시: Eunkwang Choi

| 커밋 | 내용 |
|---|---|
| `d777e24` | 초기 구현 전체(0.1.0). 소유자가 "PR 없이 커밋·푸시"를 지시해 main 에 직접 올렸다 |

*이 기록은 2026-09-19 에 같은 기기에서 당시 대화를 보고 뒤늦게 적은 것이다. 만든 것의 목록은 CHANGELOG 0.1.0 과 README 에 있고,
여기에는 코드·git 으로 알 수 없는 것만 적는다.*

- **중간에 만들었다가 소유자 지시로 지운 기능이 있다**: 검색 탭, 형광펜·사각형·선·이미지(도장·서명) 삽입 도구.
  "[검색] 메뉴는 제거한다 / [편집] 메뉴에서 형광펜, 사각형, 선, 이미지·도장·서명 삽입 단추는 제거한다"는 지시였고,
  같은 지시로 **캡처 기능**을 새로 넣었다. 지울 때 단추만 숨기지 않고 관련 코드(도형·이미지 저장 경로 포함)까지 걷어냈으며,
  **이 모든 일이 첫 커밋 전에 일어나 git 이력에 남아 있지 않다.** 되살리려면 새로 만들어야 한다(0.2.0 의 본문 찾기가 그런 경우다 — 3-1 의 D2).
- **도움말(F1)과 `src/model/ui.ts` 단일 출처 구조는 소유자 지시에서 나왔다**: "새로운 기능이 삽입되거나 있던 기능이 수정되면 반드시 반영하도록 한다."
  탭·도구·단축키를 한 곳에 두고 `guide.test.ts` 가 `App.tsx` 의 키 처리와 대조하게 한 것은 그 지시를 사람 손에만 맡기지 않으려는 장치다.
- **열기 방식은 측정 때문에 바뀌었다**: 처음에는 구간 읽기(1MB 청크 + `disableAutoFetch`/`disableStream`)로 만들었는데 196MB 파일이 6.6초 걸렸다.
  계측해 보니 PDF.js 가 문서를 열 때 마지막 쪽을 검증하면서 페이지 트리를 훑어 **파일 전체를 196회 왕복으로** 읽고 있었다(스캔본은 페이지 트리가 평평하다).
  통째 읽기로 바꾸니 0.45초 → `WHOLE_FILE_LIMIT` 1GB. 5절의 "구간 읽기 청크" 결정은 이 결론 위에 있다.
  (6.6초·0.45초는 앱 안에서 잰 값이라 디스크 읽기가 빠져 있다. `bench:open` 기준 수치는 README 의 벤치마크 표를 본다.)
- 썸네일 렌더 방식을 비교해 두었으나 **구현하지 않았다**(3-2 의 2번).

검증: 타입 검사 통과 · 단위 28개 · E2E 14개(Windows·Edge). 저장본을 다시 열어 픽셀·글자 위치로 확인했다(회전 `/Rotate`·CropBox 쪽 포함).
남긴 것: **자동화로 확인하지 못한 것** — 실제 OS 의 파일·폴더 선택 대화상자, 인쇄 미리보기, 캡처의 클립보드 복사(⧉).
성능 수치는 모두 합성 샘플 기준이고 실제 스캔본으로는 재지 않았다. 배포는 이때 제안만 했고(당시 권고는 Firebase Hosting),
실제 채택은 0.2.0 의 GitHub Pages 다.

---

## 7. 여러 명이 함께 작업하는 규칙

1. **시작할 때** `git pull --rebase` → 이 문서의 2·3절 확인. 2절의 "진행 중인 작업"에 누가 무엇을 잡고 있는지 본다.
2. **작업은 브랜치에서** 한다(`feat/…`, `fix/…`). 하루를 넘기거나 기기를 옮길 때는 끝나지 않았어도 브랜치를 푸시한다 —
   다른 데스크탑에서 이어받을 수 있는 유일한 방법이다. 커밋 메시지에 `WIP` 를 붙이고, 2절의 "진행 중인 작업"에 브랜치 이름과 남은 일을 적는다.
3. **main 에 합치기 전에** `npm run typecheck && npm test && npm run e2e`. CI 가 같은 것을 다시 돌린다.
4. **배포는 한 사람이** main 에서 수동으로 실행한다. 배포했으면 2절의 "배포" 줄을 고친다. 버전을 올렸으면 CHANGELOG·`package.json`·태그.
5. **끝낼 때** 2절을 현재 값으로 고치고, 6절 맨 위에 아래 틀로 기록을 추가해 **같은 커밋(또는 같은 PR)에** 넣는다.
   - 한 일은 CHANGELOG 와 겹치지 않게 짧게. **남긴 것(미완성·미검증·임시 조치)과 이유**를 빠뜨리지 않는다.
   - 새로 알게 된 함정은 4절에, 되돌릴 수 있는 선택은 5절에, 끝내지 못한 일은 3절에 옮긴다. 해결된 항목은 지운다.
6. AI 에이전트의 개인 메모리(`~/.claude/projects/…/memory`)는 **기기마다 따로**라 다른 작업자에게 보이지 않는다.
   다음 사람이 알아야 하는 것은 반드시 이 문서에 적는다.
7. 비밀값(토큰·서비스 계정 키)은 어떤 문서에도 적지 않는다. 필요한 권한의 **종류**만 적는다(예: "`workflow` 권한이 있는 GitHub 토큰").

기록 틀:

```markdown
### YYYY-MM-DD · 기기/OS · 작업자(에이전트면 모델과 지시한 사람)

| 커밋(또는 브랜치) | 내용 |
|---|---|
| `abc1234` | … |

한 일: …
검증: 무엇을 어디서 돌렸고 결과가 어땠는지(돌리지 못한 것은 그렇게 적는다)
남긴 것: …
```
